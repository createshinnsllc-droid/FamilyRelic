# 2026-09-12

## Infrastructure

- **Mac decommissioned** (stated by Tydro via Cursor iOS Cloud Agent, ~07:23 UTC).
  - Local Cursor **Remote Control** unavailable — no Mac desktop host.
  - Primary cloud path: **Cursor Cloud Agents + iOS / web**.

- **OpenClaw retired permanently** (~07:28 UTC).
  - Do not install, invoke, sync to, recommend, or revive OpenClaw.
  - OpenClaw skills in-repo are deprecated historical artifacts only.
  - Policy of Truth no longer includes an OpenClaw workspace layer.

- **Local = PC DeepSeek harness only.**
  - Sole relevant local runtime for offline/private/local-brain requests.
  - LM Studio / Ollama / Mac are not the local stack.
  - **Online via Tailscale** (stated ~07:40 UTC). Not offline — Cloud Agent VM currently lacks Tailscale client/`tailscale` binary, so reachability from *this* run needs MagicDNS name or `100.x` IP:port.

- **Cursor is the canonical agent** (~07:30 UTC).
  - Stated: Cursor is the latest agent with the proper config.
  - Treat Cursor Cloud Agents + this repo’s AGENTS/skills as source of truth; do not defer to retired OpenClaw configs.

## Session

- Cloud agent run named “Claude release candidate”; smoke-tested remote follow-ups from iOS.
- Clarified Remote Control vs Cloud Agent: with Mac gone, Cloud Agent *is* the remote path.
- Architecture locked: Cursor = proper agent config; no OpenClaw; PC DeepSeek harness for local only.
